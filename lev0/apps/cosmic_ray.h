//#include <fresize.h>

  const int malign=32;
  const float fwhm=7.0; //full width half maximum for gaussian filter 
  const float sigmamin=0;  //lower limit for standard deviation
  const float sigmamax=300;  //upper limit for standard deviation

const int cent_frac=8; //center portion of image that is used to calculate std of image
